!!!
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
    script(src='/javascripts/zepto.min.js')
    script(src='/socket.io/socket.io.js')
    - if (typeof user != 'undefined')
      script
        window.Pomodoro = window.Pomodoro || {};
        Pomodoro.worker = "#{user || ''}";
  body!= body
