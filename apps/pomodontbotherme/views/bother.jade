script
  var socket = io.connect('/');

  socket.on('connect', function() {
    socket.emit('watch-user', 'caleb');
  });

  socket.on('tick', function(data) {
    $('.timer').html(data);
  });

h1(class='timer')
